interface:
  display_name: "Starting Backend"
  short_description: "Start and verify the SmartReconciliation backend."
  default_prompt: "Start the SmartReconciliation backend, monitor logs for the Started line, and confirm when it is running."
